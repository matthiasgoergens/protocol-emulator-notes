codex-luna exec (gpt-6-luna), read-only sandbox, 2026-09-25, on commit 5892a8a; prompt below the verdict.

1. **CONFIRMED — Area arithmetic is mostly reproducible, but the stated tile allocation differs slightly.** The budget script and results agree on the major totals, including 126,603 µm² of non-PE logic, 293,398 µm² with no array at factor 1.5, and 658,824 µm² for 16 PEs with a 512×16 programme store. The note’s allocation is 751,632 µm², while the generated budget uses 751,641 µm²; the 9 µm² difference is immaterial. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:621), [budget.py](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/budget.py:5), [budget.txt](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/results/budget.txt:1).

2. **CONFIRMED — The 2.5-stream CRC break-even is correct for the stated comparison.** `16 × 1,115 / 7,127 = 2.50`; against the 3,621 µm² programmable 16-bit checker it is 4.92. This compares the incremental cost of enabling GF(2) in all 16 PEs with a separate unit per stream; it does not establish that the PE implementation is cheaper for the one or two extra streams the note actually names. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:342), [areas.txt](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/results/areas.txt:40).

3. **WRONG — The no-array counterfactual overstates what the budget establishes.** The note adds about 302,000 µm² of proposed additions to the 293,398 µm² no-array baseline, getting roughly 595,500 µm², or 79% of allocation. But the 154,000 µm² 1P_1024×32 SRAM is not the SRAM configuration budgeted by `budget.py` (which has only 256/512/1024×16 programme SRAM), and its area/source is not demonstrated in the cited budget. The 79% result is therefore a scenario estimate, not a result that follows from `budget.txt`. Also, the “second sequencer” entry prices the sequencer and its store separately but does not show whether the added store is halo-adjusted consistently with the baseline. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:539), [budget.py](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/budget.py:34), [budget.txt](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/results/budget.txt:52).

4. **CONFIRMED — The eight-PE option’s area arithmetic matches the budget, but its workload claims are estimates.** For 8 PEs with a 512×16 store, the table gives 487,074 µm² at factor 1.5 and 607,625 µm² at factor 2.0. The GPS “2–4× slower” statement is explicitly an extrapolation from the cited acquisition timing, not a measured eight-PE design. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:578), [budget.txt](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/results/budget.txt:30).

5. **CONFIRMED — GPS cold-start scaling is arithmetically consistent.** `139 s × (4/5) × (65.472/60) = 121.2 s`. This is a scaling estimate from the GPS prototype’s K=4 result, not an independently measured K=5, 60 MHz acquisition. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:500), [gps-hotcold README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/gps-hotcold/README.md:198).

6. **CONFIRMED — The composite-video bandwidth calculations are broadly right, with a wording ambiguity.** `256 × 240 × 50 × 2 bytes = 6.144 MB/s`, not “about 3 MB/s”; 3 MB/s corresponds to one byte per pixel or half the stated frame/field convention. The note says “hue-and-luma bytes,” which suggests two bytes per pixel, so its bandwidth figure is understated by about 2×. Separately, `256 × 240 × 50 × 4 bits = 12.288 Mbit/s` is correct for the Ethernet-to-TV calculation. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:552).

7. **CONFIRMED — The PAL and NTSC line-length figures are reasonable arithmetic estimates.** At 60 MHz, a 64 µs PAL line is exactly 3,840 clocks; the NTSC line period gives an average near 3,813.3 clocks, so alternating 3,813 and 3,814 is plausible. The ±3.3° hue error is the nearest quarter-clock phase-grid bound at 4.43 MHz. These are derived values, not measurements of video output; the note correctly flags video quality as unmeasured. Ref: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:402).

8. **WEAKENED — Several PE configurations are expressible by the RTL datapath, but the note describes them as if they were validated mappings.** The probe supports the listed operand sources, shifts, selectable `g`, gated/negated Y, ALU modes, state/pipeline writeback, valid deletion and lane/carry outputs. Thus sprite/window merge, CIC, sorting, FIR on a 1-bit sample, sigma-delta, and CRC-16 are plausible configurations. The README explicitly says the RTL has no executable specification or lockstep test, so “designed” must not be read as demonstrated. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:275), [upe.v](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/rtl/upe.v:23), [unified-pe README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/README.md:5).

9. **WRONG — The CRC-32 pair mapping is underspecified and may not match the RTL’s available wiring.** The PE RTL’s feedback input is `cb_in` only when `pairlo` is set; otherwise feedback uses that PE’s own `S[15]`. The note says the low PE takes the high PE’s `S[15]` for CRC feedback and that the high PE takes `g` from the low PE’s pair wire, but it does not specify which PE sets `pairlo`, how the high PE receives the low PE’s `g_out`, or how the data bit is injected across the pair. Those connections matter because the RTL computes feedback internally as `S-or-cb_in XOR A[0]`; a high PE’s `g` input is not automatically the low PE’s feedback. Treat the CRC-32 cell as an unproven mapping, not an established capability. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:293), [upe.v](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/rtl/upe.v:137).

10. **WRONG — The v2 ISA table does not accurately reconcile all source proposals.** `multi-proto` specifies MBX on opcode E and WAITC on F, while this table assigns D to MBX and E to WAITC; that may be a deliberate new encoding, but it is not represented as such. More concretely, the `multi-proto` capture proposal uses SHO `cap[6]` and a configurable `cpin[5:3]`, whereas this table uses pair at bit 6, cap at bit 4, and no configurable capture-pin field. The table presents its XOR-1 capture as the reconciliation, but that changes the proposal’s semantics and loses its pin-select field. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:309), [multi-proto README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/multi-proto/README.md:160).

11. **WEAKENED — The pair-mode encoding is an asserted resolution, not a demonstrated one.** USB-LS proposed pair mode at SHO bit 6 and the Ethernet work also claims complementary pairs. The table gives bit 6 to `pair` and bit 5 to `psel`; this can accommodate two pair behaviours only if the implementations really share the same pin and accumulator semantics. No cited RTL for the sequencer v2 implements or tests that merged encoding. Similarly, the note says base programmes run unchanged except HALT, but source variants use distinct meanings for SHO bits 6/5 and opcode E/F. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:305), [usb-ls README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/usb-ls/README.md:125), [multi-proto README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/multi-proto/README.md:146).

12. **WRONG — The CRC/stuffing/line-coding rationale contradicts the “protocols do not depend on the array” premise for extra streams.** It puts one CRC unit on the bit path and explicitly relies on GF(2) PEs for second and third concurrent CRC streams, while the premise says protocols must run without the array. In addition, line coding is only budgeted at two instances, but the argument that stuffing/line coding should be assists because rate changes are awkward for a lockstep array does not establish that two channels cover the protocol mixes or future combinations. The arithmetic about 813/477 µm² per probe is fine; the system-level concurrency conclusion needs a channel and mix analysis. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:356), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:365), [budget.py](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/budget.py:26).

13. **WRONG — The placement-factor verdict is too strong.** The factor 1.5 is measured on a row of eight `pe16`s, while the proposed design includes an unverified rich PE, custom segment links, assists, two gain-cell banks, and fine-delay macros. The note acknowledges the whole-chip place-and-route is still needed, yet says “fit at the measured placement factor” as if 1.5 were a measured factor for this design. The arithmetic says it fits *if* that factor applies; the measured evidence does not yet establish that assumption. Ref: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:621), [budget.py](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/unified-pe/budget.py:55).

14. **WRONG — Several mapping-table “demonstrated” labels overstate what was run on generic hardware.** Ethernet-to-TV is explicitly a behavioral-model demo with the sequencer in RTL; CAN-to-TV joins firmware through a log and awaits an ISA merge; GPS tier 1 runs its audio PEs on a model; and the FPGA evidence is not a unified-chip build. The prose headline partly discloses these qualifications, but the row-level status column can be read as demonstrating the listed generic mapping. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:466), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:475), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:488), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:493).

15. **CONFIRMED — There is a bank-count / use conflict in the GPS mapping.** The note plans two 4-kbit banks and maps GPS capture and replay to separate banks, which occupies both. The same architecture also assigns banks to Ethernet line buffering and packet/tile/table uses; those cannot be concurrent with GPS as written, and fixed single ports further constrain simultaneous uses. The note should mark those as mutually exclusive workloads or budget more banks/ports. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:447), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:462), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:433).

16. **CONFIRMED — The workload-mix thread accounting is inconsistent for M1.** M1 lists three threads (“RX parse, line copy, sync”), but the mapping table’s Ethernet-to-TV design also requires video timing, and elsewhere the demonstrated setup has receiver/parser, video timing, and renderer roles. It is unclear whether “sync” is a separate timing thread or folded into line copy; the mix does not say, so the claimed fit is not auditable. M6 similarly says the third UART bus goes to a thread while accounting for “two edge samplers” without specifying how three buses fit the two bit paths. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:514), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:469), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:519).

17. **WRONG — The GPS clock plan conflates two incompatible operating modes.** The GPS tier-2 table requires a 16.368 MHz front end and captures every fifth sample using a 20-clock period at 60 MHz; the clock section then suggests running at 65.472 MHz, which is four times the front-end clock and would make the 20-clock capture period 305.5 ns rather than 1 µs. At 65.472 MHz, one must specify a different sampling schedule or use the independently clocked sampler path. The text mentions a sampler-clocked alternative but does not reconcile it with the tier-2 mapping and its stated 3,274-bit bank. Refs: [architecture-v0.md](/home/matthias/prog/janestreet/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:105), [architecture-v0.md](/home/matthias/prog/janestreet/emulator-wt-architecture/notes/architecture-v0.md:410), [gps-hotcold README](/home/matthias/prog/janestreet/emulator-wt-architecture/prototypes/gps-hotcold/README.md:101).

Overall, the area-probe figures and most direct arithmetic check out, but the note’s central feasibility case is weaker than its wording: the placement multiplier is assumed for a materially different design, the “no-array” alternative includes an unsubstantiated SRAM line, and the ISA, CRC-32 pair, and several workload mappings lack enough detail to count as validated. The clearest concrete arithmetic issue is the likely 2× understatement of host-rendered video bandwidth if “hue-and-luma bytes” means two bytes per pixel. The design should be treated as an area and mapping hypothesis pending whole-chip implementation and explicit per-mix resource schedules.

## Prompt
You are an adversarial reviewer. Read notes/architecture-v0.md in this repository (branch "architecture") and the supporting area probes in prototypes/unified-pe/ (README.md, rtl/upe.v, rtl/assists.v, synth.sh, budget.py, results/areas.txt, results/budget.txt). The note converges many special-purpose chip prototypes into one programmable chip for a Tiny Tapeout IHP SG13G2 6x4-tile design (~0.75 mm2). Cited evidence lives under prototypes/ in this checkout (e.g. eth10-node, gps-hotcold, proto-jtag-swd, sequencer-ps2-can, usb-ls, multi-proto, fpga-ulx3s).

Your job is to find what is WRONG, not to praise. Check in particular:
1. Every number: does it match the cited file? Recompute the arithmetic (break-even 2.5 CRC streams, the budget table, the no-array counterfactual, the 8-PE option, GPS cold-start scaling, pixel bandwidths, line lengths in clocks, hue error).
2. The PE configurations in section 2.4: for each claimed cell (sprite, CRC-16, CRC-32 pair, NCO 32-bit, GPS correlator, wipe-off, sorting, CIC, FIR on 1-bit samples, sigma-delta), check against rtl/upe.v whether the op fields actually allow it (operand selects, g sources, serial input, carry, lanes, writeback). Report any cell the RTL cannot actually express, with the reason.
3. The ISA v2 encoding table in section 2.1: field collisions, and whether each proposal is represented accurately relative to the master READMEs.
4. The CRC/stuffing/line-coding decision and the segment/interconnect decision: is the reasoning consistent with the measurements, or does it contradict itself?
5. Claims marked demonstrated vs designed in the mapping table: any overclaim?
6. The budget: is the placement factor treatment honest; does the verdict follow from results/budget.txt?
7. Anything important missing or internally inconsistent across sections (clock plan vs GPS clock, bank count vs uses, thread counts per mix).
Do not modify files (read-only). Output: a numbered list of findings, each labelled CONFIRMED, WEAKENED or WRONG, with file:line references, then a one-paragraph overall verdict.
